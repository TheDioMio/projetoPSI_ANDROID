<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/default_avatar"
        android:label="@string/txt_appname_petpanion"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoANDROID">
        <activity
            android:name=".AnimalDetailsActivity"
            android:exported="false" />
        <!-- <activity -->
        <!-- android:name=".ProfileActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".SignupActivity"
            android:exported="false" /> <!-- 🟢 MenuMainActivity será a primeira a abrir -->
        <activity
            android:name=".MenuMainActivity"
            android:exported="true"
            android:theme="@style/Theme.ProjetoANDROID.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">

            <!-- ESTA É A ACTIVITY QUE INICIA O APP -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ConnectionActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>